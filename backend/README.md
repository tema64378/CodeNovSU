# Backend Workspace

## Current Layout

- `api` — the main FastAPI service for CodeNovsu

## Notes

The top-level backend folder is reserved for server-side services. Right now only the API service exists, but future workers or sandbox orchestration services can be added alongside it without mixing them into the API codebase.
